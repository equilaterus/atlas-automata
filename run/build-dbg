#!/usr/bin/env bash
set -euo pipefail

root="$(cd "$(dirname "$0")/.." && pwd)"
version="$(tr -d '[:space:]' < "$root/VERSION")"
mkdir -p "$root/bin/dbg"
cd "$root/src/mcp"
go build -gcflags="all=-N -l" -ldflags="-X main.version=$version" -o "$root/bin/dbg/atlas-mcp" .
